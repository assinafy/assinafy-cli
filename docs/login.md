# `assinafy login`

## `assinafy login`

```text
Usage: assinafy login [options]

Store an API key and account ID in a profile (interactive). Reads the global
--api-key, --account-id, --base-url flags when supplied.

Options:
  -h, --help  display help for command
```

